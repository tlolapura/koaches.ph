# Supabase — https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Server-only (never expose to the browser)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Demo coach login (after running seed + auth setup)
# Email: marco@picklekoach.com  Password: KoachesDemo1!

# Resend — https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxx
# Use onboarding@resend.dev while testing; switch to your verified domain in production
RESEND_FROM_EMAIL=PickleKoach <onboarding@resend.dev>

# Public site URL (used in progress card email links)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
